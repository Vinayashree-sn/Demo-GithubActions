name: Demo-vinayashree

on:
  push:
    branches:
      - main
jobs:
  vinayashree:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v4
     - name: addition,subtraction
       run: echo $((3+5))
            echo $((5-3))
    

   

    
